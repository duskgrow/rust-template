[package]
name = "xtask"
description = "Repository automation: hook/CI checks and scaffolding (std-only, zero dependencies)"
# Internal tooling: no semver burden, never published.
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
